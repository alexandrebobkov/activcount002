package com.example.activcount_002.db;

import android.view.LayoutInflater;
import android.view.ViewGroup;
import android.widget.TextView;

import androidx.fragment.app.DialogFragment;

import org.w3c.dom.Text;

public class ParametersDialog extends DialogFragment
{
    /*static ParametersDialog newInstance()
    {
        ParametersDialog pd = new ParametersDialog();

        return pd;
    }

    @Override
    public void onCreate()
    {
        super.onCreate();
        int style = DialogFragment.STYLE_NORMAL;
        int theme = 0;

        setStyle(style, theme);
    }

    @Override
    public View onCreateView(LayoutInflater inflater, ViewGroup container)
    {
        View v = inflater.inflate(R.layour.fragment_dialog, container, false);
        View tv = v.findViewById(R.id.text);

        ((TextView)tv).settext("Dialog");
    }*/
}
